// ============ FIRST LESSON ============

let question = confirm("Lorem ipsum?");
console.log(question)

const { number } = require("yargs");

if (confirm == true) {
  console.log("Statement is true " + confirmAge);
} else {
  console.log("Statement is false " + confirmAge);
}